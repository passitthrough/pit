export class ShopDetector {
    /*
     Find Relevant information on the page to determine if it is a shop
     and if the plugin should start run

     lang=DE
     a specific amount of currency indicators
    */

    dermineIfCurrentPageIsAShop(): boolean {
        return true;
    }
}
